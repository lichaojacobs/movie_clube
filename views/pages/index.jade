extends ../layout

block content
	.container
		.row
			each item in movies
				.col-md-2
					.thumbnail
						a(href="/movie/#{item._id}")
						.caption
							h3 #{title}
							p:a.btn.btn-primary(href="/movie/#{item._id}, role="button") 观看预告片

